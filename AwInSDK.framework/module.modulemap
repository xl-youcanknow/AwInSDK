//
//  module.map.modulemap
//  AwInSDK
//
//  Created by admin on 2025/6/11.
//
framework module AwInSDK {
    header "AWSDKApi.h"   //需要对外暴露的头文件，需要为 plubic 的文件

    header "PayData.h"  //需要对外暴露的头文件，需要为 plubic 的文件
    header "AWAdReportModel.h"  //需要对外暴露的头文件，需要为 plubic 的文件
    header "GameReportData.h"  //需要对外暴露的头文件，需要为 plubic 的文件
    header "AiHelpUserdata.h"  //需要对外暴露的头文件，需要为 plubic 的文件
    export *
}
//module <#ModuleName#> {
//    header "<#Header#>.h"
//
//    export *
//}
